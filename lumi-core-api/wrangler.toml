name = "lumi-core-api"

main = "src/index.ts"         # ← Worker のエントリーファイルに合わせて
compatibility_date = "2024-12-26"

# node:buffer / node:async_hooks みたいな Node 組み込みを使う場合
# （前に Pages では nodejs_compat つけてって警告出てたやつ）
compatibility_flags = ["nodejs_compat"]
