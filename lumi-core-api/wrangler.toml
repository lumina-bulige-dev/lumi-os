name = "lumi-core-api"
main = "src/index.ts"
compatibility_date = "2025-03-01"

kv_namespaces = [
  { binding = "INVITES", id = "xxxxx" }
]

[vars]
# ADMIN_KEYはここに直書きしない（漏れる）
# wrangler secret put ADMIN_KEY で入れる
