name = "lumi-core-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Cloudflare Workers configuration for LUMINA BULIGE API
# Endpoints: api.luminabulige.com/*
# Purpose: webhooks/wise, API endpoints

[[d1_databases]]
binding = "DB"
database_name = "lumi-db"
database_id = "placeholder" # Replace with actual D1 database ID from Cloudflare dashboard (Settings → Bindings)

[env.production]
name = "lumi-core-api"

[env.staging]
name = "lumi-core-api-staging"
